spring:
  profiles:
    active: prod
  dataSource:
    url: jdbc:postgresql://192.168.1.20:5432/vacancy
    username: vasi
    password: pass
  jpa:
    # show SQL statements in console.
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    generate_statistics: true
    baseline-on-migrate: true

server:
  port: 8443
  ssl.key-store: /home/vasi/prog/sert/keystore.p12
  ssl.key-store-password: Bazilio67
  ssl.keyStoreType: PKCS12
  ssl.keyAlias: tomcat

security.require-ssl: true
